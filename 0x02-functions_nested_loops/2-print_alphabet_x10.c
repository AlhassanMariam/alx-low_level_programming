#include "main.h"

/**
 * pr8nt alpabet 10 times
 */
void print_alphabet_x10(void)
{
	int ten;
	char la;

	for (ten = la; ten <= 9; ten++)
	{
		for (la = 'a'; la <= 'z'; la++)
			_putchar(la);
		_putchar(\n);
	}
}
