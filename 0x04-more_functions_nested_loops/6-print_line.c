#include "main.h"

/**
 * print_line - draw a straight line in the terminal
 * @n: number of line to be printed
 */
void print_line(int n)
{
	int len;

	if (len > 0)
	{
		for (len = 0; len < 0; len++)
			_putchar('_');
	}

	_putchar('\n');
}
